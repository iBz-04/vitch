[package]
name = "vit_tch"
version = "0.1.0"
edition = "2024"

[features]
default = ["doc-only"]
doc-only = ["tch/doc-only"]
download-libtorch = ["tch/download-libtorch"]

[dependencies]
tch = "0.24.0"
